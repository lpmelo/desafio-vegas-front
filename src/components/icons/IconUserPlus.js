import React from "react";
import { Icon } from "semantic-ui-react";

const IconUserPlus = ({ className }) => (
  <Icon name="user plus" className={className}></Icon>
);

export default IconUserPlus;
