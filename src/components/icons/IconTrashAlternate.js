import React from "react";
import { Icon } from "semantic-ui-react";

const IconTrashAlternate = ({ className }) => (
  <Icon name="trash alternate" className={className}></Icon>
);

export default IconTrashAlternate;
